# Application Properties
spring.application.name=iStudy

# Server Configuration
server.port=${PORT:8080}

app.base-url=https://istudy-production.up.railway.app


# Database Configuration
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${PGUSER}
spring.datasource.password=${PGPASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=10
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000

# JWT Configuration
jwt.secret=mySecretKey123456789012345678901234567890
jwt.expiration=86400000
# JWT Refresh Token Configuration
jwt.refresh.expiration=604800000


spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Tashkent

# Swagger/OpenAPI Configuration
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.tryItOutEnabled=true
springdoc.swagger-ui.filter=true
springdoc.show-actuator=false
springdoc.use-fqn=true

# Logging Configuration
logging.level.com.ogabek.istudy=DEBUG
logging.level.org.springframework.security=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# Validation Configuration
spring.jackson.default-property-inclusion=NON_NULL

# Error Handling
server.error.include-message=always
server.error.include-binding-errors=always

# Profile Configuration (uncomment for specific environments)
# spring.profiles.active=dev

# Development Profile (create application-dev.properties for dev-specific settings)
# ---
# spring.config.activate.on-profile=dev
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.jpa.show-sql=true
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Production Profile (create application-prod.properties for production settings)
# ---
# spring.config.activate.on-profile=prod
# spring.jpa.hibernate.ddl-auto=validate
# spring.jpa.show-sql=false
# logging.level.com.ogabek.istudy=INFO
# logging.level.org.springframework.security=WARN
# springdoc.swagger-ui.enabled=false